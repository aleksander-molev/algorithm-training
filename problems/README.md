# Problems

Store Java solutions grouped by pattern/topic.

Suggested structure:

```text
problems/
├── arrays/
├── hashmap/
├── two-pointers/
├── sliding-window/
├── prefix-sum/
├── binary-search/
├── stack/
├── linked-list/
├── heap/
├── intervals/
├── trees/
├── graphs/
├── backtracking/
├── greedy/
└── dynamic-programming/
```

Keep detailed learning metadata in `sessions/` and `docs/problem-history.md` instead of duplicating notes everywhere.
